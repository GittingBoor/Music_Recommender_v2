fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
essentia-tensorflow>=2.1b6.dev1110
# CPU build on purpose. inaSpeechSegmenter declares plain "tensorflow" and
# "onnxruntime-gpu" as its dependencies, which pulls ~3.8 GB of CUDA libraries
# that this image never uses. The Dockerfile installs the lock with --no-deps
# so those GPU variants stay out; keep both flags together or the image doubles
# in size. onnxruntime (CPU) is pinned in the lock as the replacement.
tensorflow-cpu>=2.12.0,<2.16.0
numpy>=1.26.0,<2.0
mutagen>=1.47.0
pyacoustid>=1.3.0
musicbrainzngs>=0.7.1
umap-learn>=0.5.6
scikit-learn==1.2.2
pandas>=2.2.0
scipy>=1.13.0
python-multipart>=0.0.9
yt-dlp>=2024.4.9
inaSpeechSegmenter>=0.7.0
pytest>=8.0.0
